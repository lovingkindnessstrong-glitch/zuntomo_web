# Task: キッズクラスページ（`/kids/`）のAstroマイグレーション

## 1. 目的
既存のキッズクラス向けページ（HTML/CSS）を、Task 01で作成された共通レイアウト（`src/layouts/Layout.astro`）を活用してAstroページ（`src/pages/kids/index.astro`）として移植・再構築する。

## 2. 前提条件
- `project_guidelines.md` を熟読し、ルーティング要件とデザインシステムを遵守すること。
- Task 01で構築された `src/layouts/Layout.astro` の構造を解析し、このレイアウトコンポーネントを利用してページを構築すること。

## 3. 実装ステップ
1. **現状分析:** 既存のキッズクラス用静的HTMLファイルを読み込み、固有のコンテンツブロックを抽出する。
2. **ページ構築:** `src/pages/kids/index.astro`（または `src/pages/kids.astro`、既存のルーティング設計に従う）を作成し、`Layout.astro` で全体をラップする。
3. **SEOメタ情報の設定:** `Layout.astro` にPropsとして以下のSEO情報を渡すこと。
   - Title: `キッズダンスクラス | ズンとも | 沼津・三島のキッズ向けダンス教室`
   - Description: （既存のHTMLから適切なものを引き継ぐか、キッズ向けに最適化）
4. **「かんなみ猫おどり」セクションの保持:** ガイドライン第7項の戦略に基づき、実績・コンバージョン要素としての「かんなみ猫おどり」参加実績セクションの構造を正確に引き継ぐこと。
5. **パスの解決:** 画像（特にキッズや猫おどりの写真）やCSSのパスが正しく解決されているか確認する。

## 4. 厳守事項（AI Agentへの警告）
- **既存デザインの完全保持:** HTMLタグ構造、CSSクラスは「そのまま」流用し、AIによる独自のUI改善は絶対に行わないこと。
- **レイアウトの再利用:** ヘッダーやフッターのコードを重複して書かず、必ず `Layout.astro` の機能を利用すること。

## 5. 完了条件（DoD）
- `npm run build` がエラーなく完了すること。
- ブラウザで `/kids/` にアクセスし、デザイン崩れがなく表示されること。
- 全ての作業と確認が完了した後、このタスクファイル自身を `docs/archive/` ディレクトリに移動させること。